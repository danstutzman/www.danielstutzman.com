---
title: Articles
filter:
- erb
- textile
---
h2. <%= h(@page.title) %>

<%
  articles = @pages.find(:all, :in_directory => "",
                         :sort_by => "mtime", :reverse => true)
  paginate(articles, 10) do |page|
%>
<%= page.inspect %>
<hr />
<% end %>

<%= link_to("Prev", @pager.prev) if @pager.prev? %>
<%= link_to("Next", @pager.next) if @pager.next? %>

